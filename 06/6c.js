let hour = 1;
let myName = "yue daiyan";
if (6 <= hour && hour <= 12) {
    console.log(`good morning ${myName} !`);
} else if (13 <= hour && hour <= 17) {
    console.log(`good afternoon ${myName} !`);
} else {
    console.log(`good night ${myName} !`);
}
